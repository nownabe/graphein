# tools/

Agent-facing tools used by Claude Code skills and subagents.

| Tool         | Description                                  |
| ------------ | -------------------------------------------- |
| `run-sql.ts` | Execute SQL against the dev or test database |
| `wait-pr.ts` | Poll a PR for CI/review status (JSON output) |
